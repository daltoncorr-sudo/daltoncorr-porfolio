# Archive

Pictures and media the site no longer uses, moved out of `site/` on 2026-09-25
so they aren't deployed with every push (about 53 MB). Paths mirror `site/`:
`archive/site/images/...` was `site/images/...`. To use one again, move it back
under `site/` and reference it from a page.
